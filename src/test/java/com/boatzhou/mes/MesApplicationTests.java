package com.boatzhou.mes;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

/**
 * 基础冒烟测试。
 *
 * <p>当前测试类保持最小可用，主要用于验证测试框架是否工作正常。</p>
 */
class MesApplicationTests {

    /**
     * 最小冒烟用例：验证 JUnit 可执行。
     */
    @Test
    void smoke() {
        Assertions.assertTrue(true);
    }
}
